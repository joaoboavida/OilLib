/*
 * The OilLib Library configuration file generated by CMake
 * from the config.hpp.in file.
 *
 * Copyright (c) 2012 by <jppm>
 */
#pragma once
#ifndef OILLIB_CONFIG_HPP
#define OILLIB_CONFIG_HPP

#define OILLIB_MAJOR_VERSION ${OILLIB_MAJOR_VERSION}
#define OILLIB_MINOR_VERSION ${OILLIB_MINOR_VERSION}
#define OILLIB_PATCH_VERSION ${OILLIB_PATCH_VERSION}
#define OILLIB_VERSION_STRING "${OILLIB_VERSION_STRING}"

// define linkage
#ifdef OILLIB_SHARED_BUILD
#   ifdef WIN32
#       ifdef OILLIB_EXPORTS
#           define OILLIB_API __declspec(dllexport)
#       else
#           define OILLIB_API __declspec(dllimport)
#       endif
#   else
#       define OILLIB_API
#   endif
#else
#   define OILLIB_API
#endif

namespace oil {

} // namespace oil

#endif // OILLIB_CONFIG_HPP

